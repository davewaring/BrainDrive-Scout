# Required: Anthropic API key for Claude analysis
ANTHROPIC_API_KEY=sk-ant-...

# Required: GitHub token for reading BrainDrive-Library
# Needs 'repo' or 'public_repo' scope depending on repo visibility
GITHUB_TOKEN=ghp_...

# Required: GitHub repo containing project files
# Format: owner/repo-name
LIBRARY_REPO=yourusername/BrainDrive-Library

# Optional: Directory for research logs (default: logs)
LOGS_DIR=logs

# Optional: Server configuration
HOST=0.0.0.0
PORT=8000
